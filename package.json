{
  "name": "boda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/css && sass src/scss/main.scss dist/css/main.css --no-source-map",
    "sass:watch": "sass src/scss/main.scss dist/css/main.css --watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
